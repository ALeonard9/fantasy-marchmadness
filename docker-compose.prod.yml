version: '3'

services:
  node-backend:
    build: backend
    ports:
      - "8080:8080"
  node-frontend:
    build: frontend
    depends_on:
      - node-backend
    ports:
      - "80:80"
    environment:
      mm_backend_host: "54.166.23.149"
      mm_backend_port: 8080
      PORT: 80
